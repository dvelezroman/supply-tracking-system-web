/** Cookie names for UI preferences (path=/, long-lived). */
export const PREF_LANG_COOKIE = 'st_lang';
export const PREF_THEME_COOKIE = 'st_theme';

/** Legacy localStorage key for language (migrated to {@link PREF_LANG_COOKIE}). */
export const LEGACY_LANG_STORAGE_KEY = 'lang';
